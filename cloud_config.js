window.SMOKEHOUSE_CLOUD = {
  // Вставь сюда Project URL из Supabase → Project Settings → API
  SUPABASE_URL: "",

  // Вставь сюда anon / publishable key. Service Role key сюда НИКОГДА не вставлять.
  SUPABASE_ANON_KEY: "",

  // Имя публичного bucket для фотографий
  BUCKET: "recipe-photos"
};
